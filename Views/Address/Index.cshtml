@model AdventureWorksInventory.Models.Address
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<div>
    <form asp-action="EditAddress" >
        <div>
            <input type="hidden" asp-for="@Model.AddressID" value="@Model.AddressID" />
        </div>
    <div>
        <label for="@Model.PostalCode"> PostalCode</label>
        <input asp-for="@Model.PostalCode"/>
    </div>
    <div>
        <label for="@Model.City">City</label>
        <input asp-for="@Model.City"/>
    </div>
    <div class="form-group">
        <input class="btn btn-primary" type="submit" value="Save" />
        <a class="btn btn-danger" asp-action="Index">Back</a>
    </div>    
    </form>
</div>